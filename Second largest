class Solution:
    def getSecondLargest(self, arr):
        # Code Here
        if len(arr)<2:
            return -1
        unique= list(set(arr))
        
        if len(unique)<2:
            return -1
            
        unique.sort()
        return unique[-2]
        
